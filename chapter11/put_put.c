// функция вывода, опеределенная пользователем
#include <stdio.h>
void put1(const char *);
int put2(const char*);

int put_put(void) {
	put1("If I wouldn't have so many money,");
	put1(" how I would can to spend,\n");
	printf("Happened %d symbols.\n",
		put2("then I never wouldn't care about fix old shoes."));
	
	return 0;
}
