// использует именованные константы из файла limit.h и float.h
#include <stdio.h>
#include <limits.h> // пределы для целых чисел
#include <float.h>  // пределы для чисел с плавающей запятой
/*
int main(void) {
	printf("Some limits of numbers for this system:\n");
	printf("Max value type of int: %d\n", INT_MAX); // 4 байта
	printf("Min value type of long long: %lld\n", LLONG_MIN);
	printf("In this system one byte = %d bites.\n", CHAR_BIT);
	printf("Max value type of double: %e\n", DBL_MAX);
	printf("Min normal value type of float: %e\n", FLT_MIN);
	printf("Precision of values type of float = %d symbols\n", FLT_DIG);
	printf("Difference between 1.00 and min values float, that more than 1.00 = %e\n", FLT_EPSILON);	
	
}*/

