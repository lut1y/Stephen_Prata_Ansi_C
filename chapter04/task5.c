#include <stdio.h>
/*
int main(void) {
	float speedLoading;
	float sizeFile;
	
	
	printf("Speed loading (mbits/sec): ");
	scanf("%f", &speedLoading);
	printf("Size of file (mbyts): ");
	scanf("%f", &sizeFile);
	
	printf("With speed loading %.2f mbits in sec file with size %.2f mbyte loads in %.2f secs.",
		speedLoading, sizeFile, sizeFile*8/speedLoading);
	
	return 0;
}*/
